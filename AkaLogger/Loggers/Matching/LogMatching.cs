

namespace AkaLogger.Matching
{
    public sealed class LogMatching
    {
        //public readonly LogCancel Cancel = new LogCancel();
        public readonly LogMatchingResult Result = new LogMatchingResult();
        public readonly LogTryMatching TryMatching = new LogTryMatching();
        //public readonly LogTryCancel TryCancel = new LogTryCancel();
    }
}
